//
//  HomeUI3Tokens.swift
//  LIMI AI Device — Home UI 3 Clean list
//

import SwiftUI

enum HomeUI3Color {
    static let canvas = Color(hex: "F7F7F5")
    static let surface = Color.white
    static let accent = Color(hex: "3A7D6A")
    static let text = Color(hex: "1C1C1C")
    static let secondary = Color(hex: "6F6F6A")
    static let divider = Color(hex: "E6E6E1")
}

enum HomeUI3Type {
    static let title = Font.system(size: 28, weight: .bold, design: .default)
    static let row = Font.system(size: 16, weight: .semibold, design: .default)
    static let caption = Font.system(size: 13, weight: .regular, design: .default)
}
